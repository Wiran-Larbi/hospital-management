<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_patient_tree_view" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">Create a new patient record</p>
        </field>
    </record>
    <!-- Patient Form View -->
    <record id="view_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="age"/>
                        <field name="gender"/>
                        <field name="contact"/>
                        <!-- Other fields as needed -->
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Patient List View -->
    <record id="view_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="age"/>
                <field name="gender"/>
                <field name="contact"/>
                <!-- Other fields you want to display in the list view -->
            </tree>
        </field>
    </record>

    <!-- Patient Kanban View -->
    <record id="view_patient_kanban" model="ir.ui.view">
        <field name="name">hospital.patient.kanban</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <kanban>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_card_content">
                                <div class="oe_kanban_card_header">
                                    <field name="name"/>
                                </div>
                                <!-- Add other fields for the kanban view -->
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
</odoo>